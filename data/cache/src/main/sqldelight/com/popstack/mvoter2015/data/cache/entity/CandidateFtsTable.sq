import com.popstack.mvoter2015.domain.candidate.model.CandidateId;

CREATE VIRTUAL TABLE CandidateFtsTable
USING fts5(
id TEXT AS CandidateId,
name TEXT
);

insertOrReplace:
INSERT OR REPLACE INTO CandidateFtsTable VALUES(:id, :name);